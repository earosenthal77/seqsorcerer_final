## Contents

### Overview: 
This document dives deaper into the code files in the rnaseq_pipeline folder. For more general information on the repository, please see the README
document in the general project folder. This folder exists to specifiy which code files docker must use to create the docker image. 

```__init__.py```:

```__main__.py```:

```alignment.py```:

```check_tool.py```:

```cli.py```:

```fastq_trimmer.py```: 

```feature_counts.py```:

```reference_genome.py```:

```seq_snip.py```:

```trimming.py```:

```validation.py```: 



